sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface (Milestone 5):
gradio>=6.9.0
# gradio>=6.9 declares huggingface-hub>=1.2, but transformers 4.57 (pulled in
# by sentence-transformers) needs huggingface-hub<1.0. Pinning to 0.36.2 keeps
# BOTH working at runtime; ignore pip's resolver warning about this.
huggingface-hub==0.36.2
# streamlit>=1.40.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
